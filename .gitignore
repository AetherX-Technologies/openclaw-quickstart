node_modules/
*.log
.DS_Store

# Credentials & secrets
.env
.env.*
credentials.json
*.key
*.pem
secrets/

# Real openclaw config (contains secrets) — commit only the example/placeholder version
openclaw-config/openclaw.json.local
openclaw-config/openclaw.json
